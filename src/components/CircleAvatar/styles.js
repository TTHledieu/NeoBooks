import { StyleSheet } from 'react-native'

const iconSize = 125

export default StyleSheet.create({
  avatar: {
    height: 125,
    width: 125,
    borderRadius: iconSize / 2,
    borderWidth: 2,
    borderColor: 'white',
    marginLeft: 'auto',
    marginRight: 'auto',
    marginTop: 10,
  },
})
